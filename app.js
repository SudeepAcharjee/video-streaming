const video = document.getElementById("video");

// Add a listener for the timeupdate event to update the video progress bar
video.addEventListener("timeupdate", updateProgressBar, false);

function updateProgressBar() {
  // Your code here
}
